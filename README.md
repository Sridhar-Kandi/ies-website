# IES Lab Website v6

```bash
npm install && npm run dev
```

## Changes from v5
- Team group photo moved from under hero to between News and Alliance sections
- Senior Researchers cards now match PhD Students size (big photo + topics + email)
- Secretary tab is now a compact 2-column grid (photo + name + role + email in single row)
