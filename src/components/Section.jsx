import{getTheme}from"../data/theme";import I from"./Icons";
export function PhotoPlaceholder({name,size=100,style={}}){const ini=name.replace(/^(Prof\.|Dr\.|Mr\.|Ms\.|Mrs\.)\s*/gi,"").split(" ").map(w=>w[0]).join("").slice(0,2).toUpperCase();const p=["#6366f1","#3b82f6","#10b981","#f59e0b","#ef4444","#8b5cf6"];const c=p[name.split("").reduce((a,ch)=>a+ch.charCodeAt(0),0)%p.length];return<div style={{width:size,height:size,borderRadius:size>80?18:10,background:`linear-gradient(135deg,${c}28,${c}12)`,display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0,fontSize:size*.3,fontWeight:700,color:c,...style}}>{ini}</div>;}
export function Section({children,dark,alt,style={}}){const t=getTheme(dark);return<section style={{background:alt?t.bgA:t.bg,padding:"68px 24px",transition:"background 0.4s",...style}}><div style={{maxWidth:1200,margin:"0 auto"}}>{children}</div></section>;}
export function SectionHeader({title,subtitle,dark}){const t=getTheme(dark);return<div style={{marginBottom:36}}><h2 style={{fontSize:"clamp(24px,4vw,36px)",fontWeight:600,color:t.tx,margin:0,lineHeight:1.2}}>{title}</h2>{subtitle&&<p style={{fontSize:14,color:t.txM,marginTop:8,lineHeight:1.6,maxWidth:540}}>{subtitle}</p>}</div>;}
export function Breadcrumbs({items,dark,navigate}){const t=getTheme(dark);return<nav style={{display:"flex",alignItems:"center",gap:6,marginBottom:24,flexWrap:"wrap"}}>{items.map((item,i)=><span key={i} style={{display:"flex",alignItems:"center",gap:6}}>{i>0&&<span style={{color:t.txD}}>{I.chevR}</span>}{item.href?<button onClick={()=>navigate(item.href)} style={{background:"none",border:"none",color:t.ac,fontSize:12,cursor:"pointer",fontFamily:"inherit",padding:0,textDecoration:"underline",textUnderlineOffset:3}}>{item.label}</button>:<span style={{fontSize:12,color:t.txD}}>{item.label}</span>}</span>)}</nav>;}
