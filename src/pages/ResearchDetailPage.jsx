import{getTheme}from"../data/theme";import D from"../data/siteData";import I from"../components/Icons";import{Section,Breadcrumbs}from"../components/Section";
export default function ResearchDetailPage({dark,navigate,areaId}){const t=getTheme(dark);const area=D.researchAreas.find(a=>a.id===areaId);if(!area)return null;
return<div style={{paddingTop:86}}><Section dark={dark}><Breadcrumbs items={[{label:"Home",href:"home"},{label:"Research Areas"},{label:area.title}]} dark={dark} navigate={navigate}/><h1 style={{fontSize:"clamp(26px,4vw,40px)",fontWeight:700,color:t.tx,margin:0,marginBottom:16}}>{area.title}</h1><p style={{fontSize:15,color:t.txM,lineHeight:1.75,maxWidth:800,marginBottom:48}}>{area.intro}</p><div style={{display:"flex",flexDirection:"column",gap:48}}>{area.tools?.map((tool,i)=><div key={i} style={{display:"flex",gap:32,flexWrap:"wrap",alignItems:"flex-start",flexDirection:i%2===1?"row-reverse":"row"}}>{tool.image&&<div style={{width:"min(100%,400px)",borderRadius:14,overflow:"hidden",flexShrink:0}}><img src={tool.image} alt={tool.title} style={{width:"100%",height:"auto",objectFit:"cover",minHeight:220}}/></div>}<div style={{flex:1,minWidth:280}}><h2 style={{fontSize:"clamp(20px,3vw,30px)",fontWeight:700,color:t.tx,margin:0,marginBottom:14}}>{tool.title}</h2><p style={{fontSize:14,color:t.txM,lineHeight:1.75,marginBottom:12}}>{tool.description}</p>{tool.usage&&<p style={{fontSize:14,color:t.txM,lineHeight:1.75,marginBottom:20}}>{tool.usage}</p>}{tool.tags&&<div style={{display:"flex",flexWrap:"wrap",gap:8}}>{tool.tags.map((tag,j)=><span key={j} style={{padding:"8px 18px",borderRadius:99,border:`1px solid ${t.bd}`,fontSize:12,color:t.tx,background:t.bgC,fontWeight:500}}>{tag}</span>)}</div>}</div></div>)}</div></Section></div>;}
